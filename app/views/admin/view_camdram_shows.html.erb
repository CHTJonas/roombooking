<% content_for :title, 'Configure Shows' %>
<% content_for :heading, 'Configure Shows' %>

<p>
  Here you can import shows from Camdram that aren't yet known to the internals of Room Booking.
  Once they're imported you can activate and decativate them, as well as set limits on the number of bookings they're allowed.
</p>

<table class="table table-condensed table-bordered table-hover">
  <thead>
    <%= content_tag :tr do %>
      <%= content_tag(:td, 'Name') %>
      <%= content_tag(:td, 'Actions') %>
    <% end %>
  </thead>
  <tbody>
    <% @shows.each do |show| %>
      <%= content_tag :tr do %>
        <%= content_tag(:td, show.name) %>
        <%= content_tag :td do %>
          <%= actions_for_show show %>
        <% end %>
      <% end %>
    <% end %>
  </tbody>
</table>
