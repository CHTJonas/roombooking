<% content_for :title, 'Rooms' %>
<% content_for :heading, 'Rooms' %>
<% content_for :controller_links do %>
  <%= link_to 'New room', new_room_path, class: 'btn btn-outline-success' %>
<% end if can? :create, Room %>

<ul>
  <% @rooms.each do |room| %>
    <%= render room %>
  <% end %>
</ul>
