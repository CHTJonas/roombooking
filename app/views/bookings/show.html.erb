<% content_for :title, @booking.name %>
<% content_for :heading, 'Booking Details' %>
<% content_for :controller_links do %>
  <p class="mb-0"><%= link_to 'Edit', edit_booking_path(@booking) %></p>
  <p class="mb-0"><%= link_to 'Destroy', booking_path(@booking), method: :delete, data: {confirm: "Are you sure you want to delete #{@booking.name}?"} %></p>
  <% if can?(:approve, @booking) && !@booking.approved? %>
    <p class="mb-0"><%= link_to 'Approve', approve_booking_path(@booking), method: :post %></p>
  <% end %>
<% end if can? :crud, @booking %>

<table class="table table-sm table-striped table-bordered">
  <tbody>
    <%= render @booking %>
  </tbody>
</table>

<% unless @booking.notes.blank? %>
  <%= content_tag(:h4, 'Notes') %>
  <%= tag.p @booking.notes %>
<% end %>

<%= render 'backfooter' %>
